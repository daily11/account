1 采用MVC设计模式

2 每一个html充当2个角色：控制器+视图。
	在html页面中，这个页面只写控件展示、控制逻辑相关的代码，不需要罗列业务逻辑代码，业务逻辑代码全部写到对应的js文档中。
	例如：index.html，则对应的逻辑处理代码写到index.js中，充当模型层！
	
3 文档命名规约：
（1）文档里面的字段命名遵循“驼峰命名”；
（2）文档命名格式：下划线分开，全部小写。
	例如：time_axis.html、date_ranges.json
（3）

4 统一采用jquery，尽量避免原生js的dom操作。

